/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains: ['picsum.photos', 
            'www.craiyon.com',
            "media.craiyon.com"
        ],
    }
};

export default nextConfig;
